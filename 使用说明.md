# A股投资分析助手 — 使用说明

**当前版本:v3.0** · 个人研究工具 · Python + AKShare/DangInvest + ECharts

> ⚠️ 本工具仅供个人研究参考,不构成投资建议。数据来自免费接口,可能有延迟或临时失效。

## 架构总览

```
                    ┌─────────────────── 数据层(韧性) ───────────────────┐
   免费数据源  ───▶ │ fetcher(双源降级+缓存+限速+Chrome指纹) · danginvest │
 东财/新浪/同花顺   │ 三层兜底:当日缓存 → 实时(主源→备源)→ 陈旧缓存兜底   │
 /DangInvest        └───────────────────────┬─────────────────────────────┘
                                             ▼
        ┌──────────── 分析层(纯代码计算,AI只解读) ────────────┐
        │ indicators(MA/MACD/RSI/量比/乖离) · scoring(6维100分) │
        │ chips(筹码CYQ) · chan(分型/笔/线段/中枢/买卖点/背离)   │
        │ candle_patterns(70形态库检测) · knowledge/kb(领域知识)│
        └───────────────────────┬───────────────────────────────┘
                                 ▼
     ┌───────── 入口 ─────────┐        ┌──────── 输出/技能 ────────┐
     │ daily_report 热点板块池 │        │ render-html 交互图文HTML  │
     │ global_scan 全市场池    │  ──▶   │ (下拉切股+4Tab+筹码+资金) │
     │ analyze 点名分析        │        │ backtest-review 复盘命中率│
     └────────────────────────┘        │ trading-memory 操盘台账   │
                                        │ news-analysis 消息面      │
                                        └───────────────────────────┘
```

**核心原则**:①AI 零计算(指标/评分/筹码/缠论全由代码算,AI 只解读);②数据韧性(双源+缓存,单源挂了不影响);③知识驱动(缠论/K线形态蒸馏成 `knowledge/kb/` 文件供代码调用);④强制反方理由 + 硬规则否决(不报喜不报忧)。

## 目录结构

```
ASharesDailyHelper/
├── config.yaml              ← 风格配置(短线/排除ST/止损/数据源),随时改无需改代码
├── scripts/
│   ├── fetcher.py           ← 数据层:双源降级 + 缓存兜底 + 限速 + 指纹伪装 + 资金流
│   ├── danginvest.py        ← DangInvest 数据源客户端(东财 clist 的完整备源)
│   ├── chips.py             ← 筹码分布(CYQ)估算:平均成本/获利比例/控盘集中度
│   ├── chan.py              ← 缠论引擎:分型/笔/中枢 + 3类买卖点 + MACD背驰 + 形态/颈线
│   ├── candle_patterns.py   ← K线组合形态检测器(据 knowledge/kb 的70形态库)
│   ├── cache.py             ← 本地缓存/落库(SQLite,data/market.db)
│   ├── indicators.py        ← 技术指标计算(MA/MACD/RSI/量比/乖离率)
│   ├── scoring.py           ← 6维度100分评分 + 硬性规则
│   ├── daily_report.py      ← 主入口:热点板块选股 → 报告(.md)+ 侧车(.json)
│   ├── analyze.py           ← 点名分析:对任意A股代码出指标+筹码+资金+侧车
│   ├── analyze_hk.py        ← 港股点名分析:复用全套引擎,东财K线失败自动切新浪,并入当日侧车
│   ├── global_scan.py       ← 全局扫描:双路(趋势追涨+超跌回调)+regime gate+RPS+涨停基因+因子
│   ├── factors.py           ← 增量因子库:筹码/资金连续性/VCP/换手/事件黑名单(影子运行)
│   ├── expand_glossary.py   ← 术语库扩充:从形态库补全+记录未命中术语
│   └── selftest.py          ← 离线自测(不联网)
├── reports/                 ← 报告输出,按日期归档:reports/YYYYMMDD/(日报.md / .json侧车 / 图表版.html / 全局池 / 个股分析)
│   └── 20260705/ …          ← organize_reports.py 自动归档;生成脚本也直接写入当日文件夹
├── reviews/                 ← 复盘输出 + 累积命中率台账(复盘 skill 写入)
├── trades/                  ← 操盘台账(操盘记忆 skill 写入,含 ledger.jsonl)
├── portfolio/               ← (二期)同花顺导出的持仓CSV
├── skills/                  ← 本项目自建 Skill
│   ├── trading-memory/      ← 操盘记忆:记录买卖/持仓/复盘 + 胜率纪律统计
│   ├── render-html/         ← 交互图文HTML:下拉切股+K线/缠论/形态Tab+筹码+资金流
│   ├── backtest-review/     ← 盘后/周度复盘:荐股 vs 真实走向,命中率与历史成功率
│   └── news-analysis/       ← 消息面:联网搜政策/热点 → 归纳 → 注入日报
├── knowledge/               ← 领域知识库
│   ├── kb/chan-rules.md              ← 缠论可执行规则(蒸馏自108课)
│   ├── kb/candlestick-patterns.json  ← 70种K线组合形态库(可编程判定)
│   ├── kb/glossary.json              ← 小白术语库(64条,报告hover解释用)
│   └── *.pdf / *.pptx                ← 原始资料
├── third_party/             ← 开源 Skill(a-share-skill / Stock-Analysis-Skill)
└── data/                    ← 本地缓存数据库 market.db
```

## 安装依赖

```
cd C:\Users\Lenovo\Claude\Projects\ASharesDailyHelper
pip install akshare pyyaml pandas curl_cffi requests
```

> `curl_cffi` 是新增关键依赖:东财接口会按 TLS 指纹拦截普通 python 请求,
> 用它伪装 Chrome 指纹才能稳定直连。

## 在哪里跑

**必须在你自己的电脑上跑**(Claude 沙箱的网络连不上行情源,你的电脑没有此限制):

```
python scripts\daily_report.py
```

首次约 1~2 分钟,报告输出到 `reports\日报-日期.md`。同一天重复运行会命中缓存、秒出。

## 日常流程

1. **早上 8:30 前 · 一键盘前**:`python scripts\run_workflow.py`
   一条命令依次跑:热点池(daily_report,板块强弱+相对强度)→ 全局池(global_scan,趋势+超跌双路)→ 合并渲染
   `盘前报告-日期.html`(大盘+板块强弱榜+今日之选+个股详析,归档到 `reports\YYYYMMDD\`)。可设 Windows 定时任务(见下)。
2. **对我说「今日热点」**:我读当天报告 + 联网搜索政策/热点,补写消息面并给综合研判(再重跑第1步即并入报告)
3. **操作后对我说「记一笔」**:我用操盘记忆 skill 把你的买卖记入台账
4. **盘后/周末对我说「复盘」**:我用复盘 skill 对照真实走向验证荐股命中率
5. 决策前,先按报告里「同花顺自查」栏自己复核一遍

> 也可以直接对我说「跑一遍盘前工作流」,我替你执行第1步并把消息面一并补上。
> 分步执行(需要时):`daily_report.py`(热点池)、`global_scan.py`(全局池)、
> `skills\render-html\scripts\report_to_html.py 侧车.json --global 全局池.json`(渲染)。

## 数据源与韧性(重要)

免费接口会限流/临时失效,本工具用**三层兜底**保证「永远跑得完」。当前板块主源已默认
`danginvest`(避开东财限流,体验更稳):

| 数据 | 主源 | 备源 | 再兜底 |
|---|---|---|---|
| 三大指数 | 新浪 | — | 缓存 |
| 板块排名 | **DangInvest** | 东财 push2 | 缓存 |
| 板块成分股 | **DangInvest** | 东财 push2 | 缓存 |
| 个股资金流 | 东财 push2his | — | 缓存/优雅缺省 |
| 个股日K | 东财 push2his | **新浪** | 缓存 |
| 涨停池 | 东财 | — | 缓存 |

- **缓存**:当天已取的数据直接复用(省请求、避限流);实时失败则回退最近一次缓存并标记陈旧,由新鲜度门禁(默认>4天拒用)把关
- **双源自洽**:板块排名走到哪个源,其成分股就用同源接口,命名不会对不上
- 即使东财**完全挂掉**,系统也能靠 DangInvest + 新浪出一份完整报告

## 常见问题:东财接口连不通 / ProxyError / Connection closed

按可能性从高到低:

1. **系统代理**:`fetcher.py` 已强制 `NO_PROXY` 直连,一般无需理会;若仍报 ProxyError,关掉系统代理再跑。
2. **东财限流(最常见)**:短时间请求过多,东财会临时断连该 IP(几十分钟量级)。表现为 `Connection closed abruptly`。应对:
   - **等**:静置一段时间会自动恢复,别反复重试(重试会续上冷却)
   - **换 IP**:重启光猫/路由器重新拨号,或临时用手机热点(封禁认 IP,换了立即生效)
   - 已默认 `prefer_source: danginvest`,板块数据**彻底不碰东财 clist**;板块图表里「领涨股」列会留空(用东财主源才有)
3. **资金流/K线偶发缺失**:走东财 push2his,限流时资金流面板自动省略、K线自动切新浪,报告照常产出。
4. 正常日用一天只跑一两次,基本不会触发限流。

## 修改配置(config.yaml)

- `prefer_source`:板块主源 `danginvest`(默认,避开东财限流) / `eastmoney`(全,含领涨股)。失败自动互备
- `danginvest_mode`:板块维度 `industry`申万大类 / `sub`同花顺细分 / `concept`概念
- `chart_bars`:图表版 K线展示根数(当前 120)
- `request_interval_sec`:接口间隔(当前 1.0,会 +0~50% 抖动)
- `max_retries`:重试次数(当前 2,压低以免限流时续封禁)
- `max_drawdown_pct`:止损容忍(当前 8) · `min_score`:入池门槛(当前 60)
- `exclude_st` / `exclude_sub_new` / `exclude_bse`:ST/次新/北交所开关
- `top_sectors` / `stocks_per_sector`:扫描广度(当前 3×5)

## 报告怎么读

- **评分 ≥75 且多头排列** = 重点关注;**60~74** = 关注
- 触发硬规则(RSI6>80 超买 / MA5乖离>5% 追高)的股**直接不进池**——这是保护
- 每只股必附:止损价(MA20 与 -8% 取高者)、支撑/压力、反方理由、同花顺自查路径
- **荐股池为空是正常输出**:当天没有低风险入场点,空仓也是操作

## 设置每日自动运行

Win+R 输入 `taskschd.msc` → 创建基本任务 → 每天 08:15 →
启动程序 `python`,参数 `scripts\daily_report.py`,起始于本项目目录。

## 四个自建 Skill

- **操盘记忆**(`skills/trading-memory/`):对我说「我买了/卖了/复盘/看我的操盘记录」,
  我会记台账并统计胜率、盈亏比、持仓天数、**止损纪律**(扛单会被标出)。
- **渲染 HTML**(`skills/render-html/`):
  - `md_to_html.py`:任意 Markdown → 富文本网页(表格/卡片/涨跌红绿/信号徽标)
  - `report_to_html.py`:日报/点名/全局 `.json` 侧车 → **交互图文版**(深色面板,下拉切股 + 4个Tab:
    **K线** / **缠论**(分型/笔/中枢/1B/3B买卖点/背驰) / **形态**(支撑压力/颈线/双顶双底) / **K线形态**(70种组合标注) + 筹码分布 + 资金流)
  - 图表基于 ECharts(CDN 加载,查看需联网);其余纯标准库,无需装依赖
- **盘后复盘**(`skills/backtest-review/`):对我说「复盘今天的荐股/本周表现/历史命中率」,
  我用荐股的目标/止损对照真实走向判定命中,累积长期成功率(先触目标=胜、先触止损=败)。
- **消息面**(`skills/news-analysis/`):对我说「今日热点/消息面」,我联网搜政策/热点/公告,
  按「板块→催化剂→情绪or逻辑→受益股」归纳并与技术面交叉,注入日报第三章。

## 验证与测试

- `python scripts\selftest.py`:离线自测指标+评分逻辑(不联网,5 项断言)
- 样例见 `reports\样例日报-离线演示数据.md`(模拟数据,股票均为虚构)

## 二期预告 / 待打磨

- 持仓诊断:同花顺导出持仓 CSV 放入 `portfolio/`,对我说「诊断持仓」(结合操盘记忆的未平仓 + 当日评分)
- 模拟盘:用 `third_party/a-share-skill/a-share-paper-trading` 建虚拟账户验证策略
- 缠论:严格线段(特征序列+缺口)、严格背驰(段力度面积)、2B/2S、多级别(大级别看势小级别下手)
- K线形态:已实现 74 种里最可靠的 17 种,可继续扩充检测器

## 更新日志(Changelog)

- **v3.0** — **港股点名分析**(`scripts/analyze_hk.py`):对港股代码(如 `01810=小米集团-W 09988=阿里巴巴-W`)复用 指标/评分/筹码/缠论/研判 全套引擎,与 A股 `analyze.py` 的 picks 并入**同一份当日侧车**(同代码覆盖),一份 HTML 同时看 A股+港股。数据韧性:日K 东财 push2his(secid `116.xxxxx`,curl_cffi 指纹直连,含换手率)→ 失败自动切**新浪前复权**(无换手率,用内置总股本表估算换手,仅影响筹码分布精度;新增港股需在 `HK_TOTAL_SHARES` 补一行股本);资金流走东财 fflow(港股同样可用),失败优雅缺省。注意:新浪为复权口径,现价建议与实盘核对;港股无涨跌停/T+0 等制度差异,评分与硬规则沿用 A股口径仅供参考。 — ①**工作流一键化**:`scripts/run_workflow.py` 依次跑 热点池→全局池→合并渲染盘前报告,一条命令替代分步(消息面仍由「今日热点」单独触发);②config 新增 `exclude_star` 排除**科创板**(688/689,无购买资格),接入热点池与全局池两处过滤。
- **v2.8** — 两项内核升级:①**板块强弱榜 + 板块内相对强度**(`analyze_sector_strength`):板块不只看涨跌幅,综合动量+**广度(上涨家数占比)**+中位涨幅打强弱分(广度高=普涨真强,只领涨股涨=假强);热点池选"高于板块平均结构"的个股(相对强度)。②**研判合成器**(`scripts/judge.py`,接入热点池/全局池/点名分析三入口):**结构化止损**(取现价下方最近有效支撑:前低/MA20/MA10/缠论中枢下沿/缠论支撑 - 缓冲,而非硬套-8%;止损过远预警)、基于结构止损的**真实盈亏比**(<1判不值博)、**一句话立场**、**矛盾点**(趋势多头但RSI高/量价背离/浮盈盘重/逆势吸筹…)、**灰色地带预警**(RSI/乖离/换手接近危险区)、带价位的**介入/失效**;报告新增**今日之选**(按机会质量分置顶1-2只聚焦),研判决策卡替代指标罗列。目的:从"指标枚举器"转向"有立场的研判助手"。
- **v2.7** — 修"只推趋势追涨"三层根因:①**双候选入口**(`_prefilter` 拆趋势按成交额/超跌按当日跌幅深两路取候选,根治"成交额榜天然只有强势股"把超跌股挡在门外);②**防守档超跌路不再关闭**,改"严格影子"(仅收底背离1B/见底K线强企稳信号),`regime` 返回 `oversold_strict`;③修 **regime 缓存过期 bug**(改判据后旧缓存导致超跌路仍被跳过)。实测防守档下超跌路捞出圣泉集团/中天科技/TCL中环等超跌见底候选,合并报告不再清一色趋势追涨。**认知对齐**:6维评分是趋势打分器,超跌反弹/底部反转/横盘吸筹靠独立策略路+企稳判据捕获,而非评分门槛。
- **v2.6** — 选股增量优化(第二+三梯队,`scripts/factors.py`):**筹码转筛选**(突破时获利<60%加分、获利>90%高位放量否决)、**主力资金连续性**(连续3日净流入+横盘=吸筹加分,价涨+主力流出=诱多剔除)、**VCP/箱体平台突破**、**换手率区间**(5~15%活跃加分)、**事件黑名单**(盘前拉解禁/业绩雷全市场清单剔除)。全部**影子运行**:命中因子写入侧车 `factors`,`backtest-review stats` 新增 by_strategy/by_factor 分组命中率,数据验证后再转正。**术语库闭环**:`expand_glossary.py` 从K线形态库自动补全术语(64→132条),渲染时未命中术语记入 `glossary_misses.json`(K线形态术语现已全部可 hover 解释)。
- **v2.5** — 一份合并报告:热点池+全局池+消息面整合到单个 HTML(下拉带【池】标签,不再分散多文件);研判文字统一 icon+副标题+说明版式;新增**操作建议模块**(短线+波段的建仓/加减仓/做T/波段倾向 + 依据 + 反方失效 + 波段视角,明确"基于历史数据非实时喊单")。
- **v2.4** — 选股增量优化(第一梯队):**大盘环境总开关 regime gate**(上证vs MA20/MA60 定进攻/谨慎/防守,弱市自动提高门槛+减半持仓+关超跌路,热点池与全局池共用);全局池**双路扫描**(趋势追涨 + 超跌回调影子);趋势路加 **RPS相对强度**(全市场60日涨幅百分位≥85)与**涨停基因**(近60日涨停数排序);config 加 `exclude_chinext` 排除创业板、`trading_style: 短线+波段`。
- **v2.3** — 报告按日期归档(`reports/YYYYMMDD/`,新增 `organize_reports.py` 一键整理,生成脚本直接写入当日文件夹,便于追溯);资金流四档、动态副指标、术语hover等 v2.2 特性沿用。
- **v2.2** — 图表版重构:动态副指标区(默认MACD,+号增至3个,下拉切MACD/RSI/KDJ/WR/量/BIAS不重复,严格对齐K线)+ 主图叠加BOLL;修复筹码图均本/现价线刻度错位(类目轴改按索引定位);资金流改**超大/大/中/小单四档独立**(机构/游资/中户/散户,不再镜像);个股分析报告去大盘、个股详析前置;研判文字关键指标**鼠标悬浮小白解释+事例**(接 `glossary.json` 64条术语库,子Agent蒸馏)。
- **v2.1** — 补线段层(工程近似)+ 用线段重构中枢 + 双护栏(拒退化中枢);图表改全屏宽版、图表加高自适应;缠论 Tab 加"线段"叠加。
- **v2.0** — 交互式图文报告(下拉切股 + K线/缠论/形态/K线形态 4 Tab);缠论引擎(分型/笔/中枢/3B/3S/MACD背离1B/1S);筹码分布(CYQ)+ 资金流(主力vs散户);K线形态检测器(接 70 形态知识库);全市场清单周级缓存;缠论/K线形态知识库蒸馏(`knowledge/kb/`);新增操盘记忆/渲染HTML/盘后复盘/消息面 4 个 Skill。
- **v1.0** — MVP:热点板块选股 + 6维100分评分 + 硬规则否决 + 每日报告(md);双源降级(东财⇄DangInvest⇄新浪)+ 缓存兜底 + curl_cffi 指纹直连。

> 开源提示:数据源均为第三方免费接口,请遵守其使用条款;`knowledge/` 下的书籍/PPT 为个人学习资料,开源时请勿一并分发(仅保留蒸馏出的规则文件或注明来源)。
